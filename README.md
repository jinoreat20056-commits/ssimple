# Jenkins Docker Demo

Simple Flask application used to demonstrate:

GitHub → Jenkins → Docker Image → Docker Container

## Run locally

pip install -r requirements.txt

python app.py

Open:

http://localhost:5000
